# AI 深入解读功能需要的环境变量 —— 复制这些「名称」到 Vercel 后台配置，
# 真正的密钥值只填在 Vercel，绝不要写进代码或提交到仓库。
#
# 在 Vercel：项目 → Settings → Environment Variables 逐个添加。

# 必需：你的 Claude API 密钥（从 https://console.anthropic.com 获取）
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 可选：允许调用接口的来源，默认 https://lenormand-rho.vercel.app
# 换成自定义域名时改这里（不含结尾斜杠）。
ALLOWED_ORIGIN=https://lenormand-rho.vercel.app
